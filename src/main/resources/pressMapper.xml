<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meditation.dao.pressDao">
    <select id="query_press" parameterType="string" resultType="com.meditation.pojo.press">
        SELECT * FROM press WHERE id = #{id};
    </select>
</mapper>